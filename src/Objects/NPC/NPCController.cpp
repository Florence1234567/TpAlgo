#include "NPCController.h"

void NPCController::HandleEvent(const sf::Event& event, sf::FloatRect playingBounds, sf::RenderWindow* window)
{
}

void NPCController::Update(sf::Time dt)
{
	Controller::Update(dt);
}
