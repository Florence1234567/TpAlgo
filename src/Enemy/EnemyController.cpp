#include "EnemyController.h"

void EnemyController::HandleEvent(const sf::Event& event, sf::FloatRect playingBounds, sf::RenderWindow* window)
{
}

void EnemyController::Update(sf::Time dt)
{
	Controller::Update(dt);
}
